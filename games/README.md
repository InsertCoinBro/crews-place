# Games

Each game gets its own folder so it can grow without tangling with another activity.

When we begin a game, copy `_template`, rename the copied folder, and build inside it. Shared behavior belongs in `../shared/` only when two or more activities need it.

